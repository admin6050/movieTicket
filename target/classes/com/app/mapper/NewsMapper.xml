<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.NewsMapper">
    <resultMap id="BaseResultMap" type="com.app.entity.News">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="publishdate" jdbcType="TIMESTAMP" property="publishdate"/>
        <result column="author" jdbcType="VARCHAR" property="author"/>
    </resultMap>

    <select id="queryAllCount" resultType="int" parameterType="String">
        select count(*) from news where title like concat('%',#{title},'%')
    </select>

    <select id="listMovie" resultType="com.app.entity.News" >
        select * from news where title like concat('%',#{title},'%') limit #{begin},#{end}
    </select>


    <select id="getById" resultType="News" parameterType="Integer">
        select * from news where id=#{id}
    </select>

    <insert id="add" parameterType="news">
        insert into news
        <set>
            <if test="title!=null and  title!=''">
                title=#{title},
            </if>
            <if test="author!=null and  author!=''">
                author=#{author},
            </if>
            <if test="content!=null and  content!=''">
                content=#{content},
            </if>
            <if test="publishdate!=null">
                publishdate=#{publishdate},
            </if>
        </set>
    </insert>

    <update id="update" parameterType="news">
        update news
        <set>
            <if test="title!=null and  title!=''">
                title=#{title},
            </if>
            <if test="author!=null and  author!=''">
                author=#{author},
            </if>
            <if test="content!=null and  content!=''">
                content=#{content},
            </if>
            <if test="publishdate!=null">
                publishdate=#{publishdate},
            </if>
        </set>
        where id=#{id}
    </update>

    <delete id="delete" parameterType="Integer">
        delete from news where id=#{id}
    </delete>

    <select id="latestListNews" resultType="com.app.entity.News" >
        select * from news order by publishdate desc limit 0,3
    </select>
</mapper>